apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "franz.fullname" . }}-test-connection"
  labels:
    {{- include "franz.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "franz.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
